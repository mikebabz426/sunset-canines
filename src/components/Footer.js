import React from "react"
import FooterStyles from "../components/Footer.module.scss"

const Footer = () => {
  return (
    <div className={FooterStyles.footer}>
      <h3 className={FooterStyles.heading}>This is the footer</h3>
    </div>
  )
}

export default Footer
