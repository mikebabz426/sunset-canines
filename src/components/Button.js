import React from "react"
import ButtonStyles from "./Buttons.module.scss"

const Button = () => {
  return <button className={ButtonStyles.callToAction}>Call to Action</button>
}

export default Button
