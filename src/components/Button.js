import React from "react"
import ButtonStyles from "./Buttons.module.scss"

const Button = () => {
  return <button className={ButtonStyles.callToAction}>What We Offer</button>
}

export default Button
